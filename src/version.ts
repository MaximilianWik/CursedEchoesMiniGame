/**
 * Single source of truth for the app's user-facing version string.
 * Surface this in menu / pause / settings / game-over / victory footers, and
 * keep it in lock-step with the top CHANGELOG entry.
 */

export const APP_VERSION = '0.3.10';
